{
  "name": "dojo-redirect",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.26.0",
    "jest": "^29.7.0",
    "wrangler": "^3.60.3"
  },
  "type": "module"
}